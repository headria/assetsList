export type ArabCoin = {
  from: string;
  to: string;
  network: string;
  amount_network: string;
  amount_arb: number;
  hash: string;
};

export type TotalBalanceArab = { totalAmount: number; _id: object };
